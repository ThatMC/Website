var config = {
    creator : "ThatMC",           // DON'T CHANGE 
    server_ip: "that.mcnetwork.me"                     // YOUR SERVER IP 
}


$('span[id="creator"]').html(config.creator);
$('[id="server-ip"]').html(config.server_ip);